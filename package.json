{
  "name": "cabc-volley",
  "private": true,
  "description": "Site du C.A. Brive Corrèze Volley — pages statiques servies par Cloudflare Workers",
  "scripts": {
    "check": "node scripts/check-content.mjs && node scripts/check-design-system.mjs && node scripts/check-echappement.mjs && node scripts/check-ffvb.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.120.1"
  }
}
